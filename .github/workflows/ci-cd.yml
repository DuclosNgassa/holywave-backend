name: Docker Build -> Push -> Deploy

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set image name
        id: vars
        run: echo "image_name=my-docker-image:latest" >> $GITHUB_OUTPUT

      - name: Use image name
        run: echo "Image is ${{ steps.vars.outputs.image_name }}"

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin' # See 'Supported distributions' for available options
          java-version: '21'

      - name: Build jar
        run: ./mvnw clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build image
        run: docker build -t ${{secrets.DOCKER_USERNAME}}/${{ steps.vars.outputs.image_name }}

      - name: Push Image
        run: docker push ${{secrets.DOCKER_USERNAME}}/${{ steps.vars.outputs.image_name }}

      - name: SSH and Deploy to EC2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{secrets.EC2_HOST}}
          username: ${{secrets.EC2_USER}}
          key: ${{secrets.EC2_SSH_KEY}}
          script: |
            docker stop holywave-app || true
            docker rm holywave-app || true
            docker pull ${{secrets.DOCKER_USERNAME}}/${{ steps.vars.outputs.image_name }}
            docker run -d --name holywave-app -p 8080:8080  ${{secrets.DOCKER_USERNAME}}/${{ steps.vars.outputs.image_name }} 
